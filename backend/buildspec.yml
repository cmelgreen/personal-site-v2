version: 0.2

phases:
  build:
    commands:
      - cd backend
      - docker build -t server .

  post_build:
    commands:
      - docker push 010629071893.dkr.ecr.us-east-1.amazonaws.com/test

artifacts:
  files:
    - 'appspec.yml'
    - 'scripts/*'
  base-directory: backend

cache:
  paths:
    - '/go/pkg/**/*'