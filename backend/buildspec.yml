version: 0.2

phases:
  # pre_build:
  #   commands:

  build:
    commands:
      - cd backend/go
      - go get -d ./...
      - go build
artifacts:
  files:
    - '**/*'

  base-directory: backend/go